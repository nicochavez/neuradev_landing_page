import type { Translations } from '../translations'
import TeamSection from '../components/Team/TeamSection'

export default function Team({ t }: { t: Translations }) {
  return <TeamSection t={t} />
}


